{
  "name": "{{NAME}}",
  "version": "0.0.0",
  "description": "{{DESCRIPTION}}",
  "main": "index.js",
  "dependencies": {
    "ejs": "^2.6.1",
    "eslint": "^5.6.0",
    "eslint-config-google": "^0.10.0",
    "node-sass": "^4.9.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
